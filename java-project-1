pipeline{
    agent none
    stages{
        stage('git clone'){
            agent {label 'node1'}
            steps{
                git branch: 'main', url: 'https://github.com/naveen13dev/onlinebookstore.git'
            }
        }
    }
}
